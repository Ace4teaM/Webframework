
;-------------------------------------------------------------------------
; SQL
; Define your application sql files here.
; This is merged with anothers applications
;-------------------------------------------------------------------------

[sql_tables]
; application = "${application_path}/../sql/postgresql/tables.sql"

[sql_func]
; application = "${application_path}/../sql/postgresql/func.sql"

[sql_init]
; application = "${application_path}/../sql/postgresql/init.sql"

[sql_populate]
; application = "${application_path}/../sql/postgresql/jeu_essai.sql"

[sql_remove]
; application = "${application_path}/../sql/postgresql/remove.sql"
